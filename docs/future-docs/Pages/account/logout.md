# صفحه خروج (Logout)

**مسیر احتمالی:** `/auth/logout/` یا `/account/logout/`

**کاربرد:**
این صفحه برای خروج کاربر از حساب کاربری استفاده می‌شود. معمولاً پس از خروج، کاربر به صفحه اصلی یا صفحه ورود هدایت می‌شود.

**نکات مهاجرت به دات‌نت:**
- پاک‌سازی سشن و کوکی‌ها.
- ریدایرکت مناسب پس از خروج. 